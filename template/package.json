{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "private": true,
  "scripts": {
    "dev": "nodemon -w server.js -w nuxt.config.js -w api/ server.js",
    "build": "nuxt build",
    "start": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "express": "^4.14.0",
    "nuxt": "latest"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
